BEFORE / AFTER SLIDER UPDATE

The Cartoon Shader devlog now uses an overlaid before/after comparison:
- cartoon-shader1.png = SHADER OFF
- cartoon-shader2.png = SHADER ON

The visitor can click/touch and drag anywhere across the image.
Left/right arrow keys also move the divider.

For perfect alignment, keep both images at exactly the same pixel dimensions.
